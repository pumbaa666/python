$('.datepicker').datepicker({
    format: 'dd.mm.yyyy',
    startDate: '-1d',
    endDate: '+5d',
    autoclose: true,
    weekStart:1,
});
$('.datepicker').datepicker('setDate', 'now');

$('.datepicker-search').datepicker({
    format: 'yyyy-mm-dd',
    autoclose: true,
    weekStart:1
});
